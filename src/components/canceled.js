import React from "react";

const Canceled = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        color: "green",
      }}
    >
      <h1>Canceled</h1>
    </div>
  );
};

export default Canceled;
